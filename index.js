module.exports = function(app) {
  app.proto.formatDate = formatDate;
  app.proto.formatTime = formatTime;
  app.proto.formatRelative = formatRelative;
  app.proto.formatNumber = formatNumber;
  app.proto.formatMessage = formatMessage;
}

function formatDate(date, locale, options) {

};

function formatTime(date, locale, options) {

};

function formatRelative(date, locale, options) {

};

function formatNumber(date, locale, options) {

};

function formatMessage(date, locale, options) {

};
